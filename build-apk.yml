name: Build APK

# Builds the installable app and attaches it to a Release, so there is a
# permanent link you can open on your phone.
#
# IMPORTANT: this file only works when it lives at
#   .github/workflows/build-apk.yml
# GitHub ignores workflow files anywhere else. A copy at the repository root
# is inert.

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write        # needed to publish the Release
  issues: write          # needed to file the failure report below

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      # The project may sit at the repository root or nested a few folders
      # down, depending on how the zip was unpacked before uploading.
      # Rather than hardcode a path that silently breaks, find the marker
      # file and derive everything from it.
      - name: Locate the project
        id: find
        run: |
          MARKER=$(find . -name sync_android.sh -not -path './.git/*' | head -1)
          if [ -z "$MARKER" ]; then
            echo "::error::Could not find sync_android.sh anywhere in the repo."
            echo "Files at the root:"; ls -la
            exit 1
          fi
          DIR=$(dirname "$MARKER")
          echo "dir=$DIR"          >> "$GITHUB_OUTPUT"
          echo "android=$DIR/android" >> "$GITHUB_OUTPUT"
          echo "Project found at: $DIR"
          if [ ! -f "$DIR/android/settings.gradle.kts" ]; then
            echo "::error::$DIR/android/settings.gradle.kts is missing — the upload is incomplete."
            exit 1
          fi

      # BOOTSTRAP — remove once these are committed to the repo.
      #
      # The manifest referenced @mipmap/ic_launcher while res/ contained no
      # icon at all, which fails resource linking on every build. Doing it
      # here avoids hand-creating nested folders in a web editor. Both
      # halves are idempotent: once the real files are committed this step
      # finds nothing to do and prints "already correct".
      - name: Ensure the launcher icon exists
        working-directory: ${{ steps.find.outputs.android }}/app/src/main
        run: |
          set -e
          changed=0
          mkdir -p res/drawable
          if [ ! -f res/drawable/ic_launcher_foreground.xml ]; then
            cat > res/drawable/ic_launcher_foreground.xml <<'ICON'
          <?xml version="1.0" encoding="utf-8"?>
          <vector xmlns:android="http://schemas.android.com/apk/res/android"
              android:width="108dp" android:height="108dp"
              android:viewportWidth="108" android:viewportHeight="108">
              <path android:fillColor="#120C24" android:pathData="M0,0h108v108h-108z" />
              <path android:fillColor="#8B7BFF" android:pathData="M54,10a44,44 0 1,0 0.1,0z" />
              <path android:fillColor="#5C4BC4" android:pathData="M54,18a36,36 0 1,0 0.1,0z" />
              <path android:fillColor="#120C24" android:pathData="M54,41a13,13 0 1,0 0.1,0z" />
              <path android:strokeColor="#EFEBFF" android:strokeWidth="3"
                  android:strokeAlpha="0.7" android:fillColor="#00000000"
                  android:pathData="M28,34 A33,33 0 0,1 62,20" />
          </vector>
          ICON
            sed -i 's/^          //' res/drawable/ic_launcher_foreground.xml
            echo "created res/drawable/ic_launcher_foreground.xml"
            changed=1
          fi
          if grep -q '@mipmap/ic_launcher' AndroidManifest.xml; then
            sed -i 's|@mipmap/ic_launcher_round|@drawable/ic_launcher_foreground|g' AndroidManifest.xml
            sed -i 's|@mipmap/ic_launcher|@drawable/ic_launcher_foreground|g' AndroidManifest.xml
            echo "repointed the manifest icon away from the missing mipmap"
            changed=1
          fi
          # Chaquopy is published from its own Maven repository as well as
          # Maven Central, and which versions live where has varied. Adding
          # it costs nothing when unused and removes a failure mode that
          # kills Gradle within seconds of startup.
          SETTINGS=../../../settings.gradle.kts
          if [ -f "$SETTINGS" ] && ! grep -q 'chaquo.com/maven' "$SETTINGS"; then
            python3 - "$SETTINGS" <<'PATCH'
          import re, sys
          p = sys.argv[1]
          s = open(p).read()
          s = s.replace("gradlePluginPortal()",
                        'gradlePluginPortal()\n        maven { url = uri("https://chaquo.com/maven") }', 1)
          s = re.sub(r"(dependencyResolutionManagement \{.*?repositories \{\n)",
                     r'\1        maven { url = uri("https://chaquo.com/maven") }\n',
                     s, count=1, flags=re.S)
          open(p, "w").write(s)
          PATCH
            sed -i 's/^          //' /dev/null 2>/dev/null || true
            echo "added chaquo.com/maven to settings.gradle.kts"
            changed=1
          fi

          # Chaquopy has no Python 3.12 build for 32-bit ARM, and listing
          # that ABI fails at configuration time:
          #   "Python 3.12 is not available for the ABI 'armeabi-v7a'."
          # Every Android phone since ~2017 is 64-bit, so dropping it costs
          # nothing real.
          APPGRADLE=../../../app/build.gradle.kts
          if [ -f "$APPGRADLE" ] && grep -q 'armeabi-v7a' "$APPGRADLE"; then
            sed -i 's/, *"armeabi-v7a"//; s/"armeabi-v7a" *, *//' "$APPGRADLE"
            echo "dropped armeabi-v7a — unsupported by Chaquopy on Python 3.12"
            grep -n 'abiFilters' "$APPGRADLE" || true
            changed=1
          fi

          [ "$changed" = "0" ] && echo "already correct — nothing to do"
          echo "--- icon reference now:"
          grep -n 'android:icon' AndroidManifest.xml || true

      # AGP 8.7 requires JDK 17. The runner ships several; pin it.
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # The pipeline lives in two places; this copies it into the APK bundle
      # and fails if a non-stdlib import has crept in, which would need a pip
      # block in Gradle and a wheel that exists for Android.
      # The runner already has python3.12 and the pipeline needs nothing
      # beyond the standard library, so there is no setup-python step.
      - name: Sync pipeline into the app bundle
        working-directory: ${{ steps.find.outputs.dir }}
        run: |
          echo "::group::Sync pipeline into the app bundle"
          chmod +x sync_android.sh
          ./sync_android.sh
          echo "::endgroup::"

      # Catches a broken pipeline here rather than after you have installed it.
      - name: Run the test suites
        working-directory: ${{ steps.find.outputs.dir }}
        run: |
          echo "::group::Run the test suites"
          for f in test_corpus.py test_adversarial.py test_comps.py test_economics.py; do
            echo "--- $f"
            python3 "$f"
          done
          echo "::endgroup::"

      # Gradle is installed directly rather than through an action.
      #
      # Two attempts went wrong here already: setup-gradle without
      # `gradle-version` provides no `gradle` binary at all (it only
      # configures caching for an existing wrapper), and adding
      # `gradle-version` introduced a different failure. Downloading the
      # distribution is boring, deterministic, and does exactly one thing —
      # worth more than convenience on a step that has now broken twice.
      - name: Install Gradle 8.9
        run: |
          set -eux
          curl -sSL --retry 3 --fail \
            https://services.gradle.org/distributions/gradle-8.9-bin.zip \
            -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /opt
          /opt/gradle-8.9/bin/gradle --version
          echo "/opt/gradle-8.9/bin" >> "$GITHUB_PATH"

      # Diagnostics only. `always()` so a build failure still shows what the
      # toolchain actually was, and `continue-on-error` so this can never be
      # the thing that fails the run.
      - name: Toolchain versions
        if: always()
        continue-on-error: true
        run: |
          echo "java:   $(java -version 2>&1 | head -1)"
          echo "gradle: $(gradle --version 2>/dev/null | grep -i '^Gradle' || echo 'NOT ON PATH')"
          echo "sdk:    ${ANDROID_HOME:-unset}"
          echo "ndk dir contents:"
          ls "${ANDROID_HOME}/ndk" 2>/dev/null || echo "  (no ndk installed)"

      # Output is teed to a file so the failure can be republished to the
      # job summary below. Reading a CI failure should not require knowing
      # how to navigate a CI log.
      - name: Build debug APK
        working-directory: ${{ steps.find.outputs.android }}
        run: |
          set -o pipefail
          gradle assembleDebug --stacktrace --no-daemon 2>&1 | tee /tmp/build.log

      # Publish the failure somewhere readable without opening a CI log.
      #
      # Four fixes were made here from inferred causes -- timings, step
      # numbers -- because the error text never reached the person who
      # needed it. An issue in a public repository is readable by anyone
      # helping, arrives as a notification, and needs no log navigation.
      - name: Report the failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          REPORT=/tmp/report.md
          {
            echo "Automated build failure report. Close this issue freely;"
            echo "a new one is filed on each failed run."
            echo ""
            echo "### Toolchain"
            echo '```'
            echo "java:   $(java -version 2>&1 | head -1)"
            echo "gradle: $(gradle --version 2>/dev/null | grep -i '^Gradle' || echo 'NOT ON PATH')"
            echo "sdk:    ${ANDROID_HOME:-unset}"
            echo "ndk:    $(ls "${ANDROID_HOME}/ndk" 2>/dev/null | tr '\n' ' ' || echo none)"
            echo '```'
            echo ""
            echo "### Gradle output (last 200 lines)"
            echo '```'
            if [ -f /tmp/build.log ]; then
              tail -n 200 /tmp/build.log
            else
              echo "No gradle log — the run failed BEFORE the build step started."
            fi
            echo '```'
            echo ""
            echo "### settings.gradle.kts"
            echo '```kotlin'
            cat "${{ steps.find.outputs.android }}/settings.gradle.kts" 2>/dev/null || echo "(not found)"
            echo '```'
            echo ""
            echo "### app/build.gradle.kts"
            echo '```kotlin'
            cat "${{ steps.find.outputs.android }}/app/build.gradle.kts" 2>/dev/null || echo "(not found)"
            echo '```'
          } > "$REPORT"

          cat "$REPORT" >> "$GITHUB_STEP_SUMMARY"
          gh issue create \
            --title "Build failure $(date -u +'%Y-%m-%d %H:%M UTC')" \
            --body-file "$REPORT" || echo "could not file an issue; see the summary above"

      - name: Name the output
        id: name
        run: echo "stamp=$(date +'%Y%m%d-%H%M')" >> "$GITHUB_OUTPUT"

      # Available from the Actions run page. Artifacts expire after 90 days
      # and require being signed in, which is awkward on a phone — hence the
      # Release below.
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: spine-apk-${{ steps.name.outputs.stamp }}
          path: ${{ steps.find.outputs.android }}/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 90

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ steps.name.outputs.stamp }}
          name: Spine ${{ steps.name.outputs.stamp }}
          body: |
            Debug build. Open this page on your phone and tap the `.apk`
            below to install. Android will ask once whether to allow
            installs from your browser.

            Prices are synthetic until a real comp source is configured —
            the app shows a banner while that is the case.
          files: ${{ steps.find.outputs.android }}/app/build/outputs/apk/debug/app-debug.apk
